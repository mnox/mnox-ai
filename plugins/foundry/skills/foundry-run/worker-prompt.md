# Foundry Worker Prompt Decommissioned

This helper belonged to the predecessor Foundry V0 loop. It is intentionally inert. Use `$foundry-run` or `$unit`, which route through Sven's native Unit runtime at `/Users/matt.noxon/dev/personal/sven/src-tauri/target/debug/sven_unit_workflow`. New work must not read predecessor Foundry stores.
