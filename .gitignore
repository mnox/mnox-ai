# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# OS / editor cruft
.DS_Store
*.swp
.idea/
.vscode/

# Logs / scratch
*.log
.tmp/

# Node / Bun (session-tracker util)
node_modules/
.bun/
*.tsbuildinfo

# Local runtime data — never commit session databases
*.db
*.db-shm
*.db-wal

# Foundry — user-created live registry (may hold private repo paths) + run-state.
# The committed template is repos.example.json; the live copy stays local.
plugins/foundry/skills/foundry-run/scripts/repos.json
.foundry/

# Scratch outputs — not repo content. Canonical homes live elsewhere (e.g. dawks
# spaces). Agentic-OS / Sven strategy artifacts land here as drafts; never ship.
artifacts/
