{
  "description": "PreToolUse gate over Bash (auto-allows provably-safe commands, defers everything else to the normal permission flow) plus a one-time SessionStart onboarding nudge that flags an installed-but-inert gate. Both hooks always exit 0 — neither ever blocks.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/bash_gate.py\"",
            "timeout": 15
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "matcher": "startup|resume",
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/bash_gate_onboard.py\"",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
